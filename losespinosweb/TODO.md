# TODO: Fix LandingService Errors in Main

## Tasks
- [ ] Update `landing_page.dart` to accept `LandingService` and use its `slides` instead of hardcoded data
- [ ] Update `cuenta_page.dart` to include admin landing management card if user is admin
